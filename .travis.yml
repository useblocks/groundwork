language: python
python:
  - "3.4"
  - "3.5"
before_install:
  - pip install pytest pytest-cov click blinker jinja2
# command to install dependencies
# install: "pip install -r requirements.txt"
# command to run tests
script: py.test